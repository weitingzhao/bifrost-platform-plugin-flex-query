# Example only — do not commit real tokens.
# Apply via: make sync-flex-tokens  (reads IB_FLEX_*_TOKEN from .env)
apiVersion: v1
kind: Secret
metadata:
  name: bifrost-flex-tokens
  namespace: plugin-flex-query
  labels:
    app.kubernetes.io/part-of: bifrost-flex-query
    bifrost.io/wave: "4"
type: Opaque
stringData:
  FLEX_HOST_TOKEN: ""
  FLEX_SECONDARY_TOKEN: ""
